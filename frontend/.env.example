VITE_TURNSTILE_SITE_KEY=
# Only needed for standalone `npm run dev` (no Django in front of it) - points
# API calls at a separately running Django dev server instead of the Vite one.
# Just the origin, no /api suffix (that's appended automatically).
VITE_API_URL=http://localhost:8000
